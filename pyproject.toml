[tool.poetry]
name = "powersys"
version = "0.1.0"
description = "An open-source power system reliability package."
authors = ["Isaac Bromley-Dulfano <ijbd@umich.edu>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
